= hidden_field_tag :direction, params[:direction]
= hidden_field_tag :sort, params[:sort]
 
%table#devices
  %tr.pretty.ajaxifyme
    %th=sortable "exten"
    %th=sortable "user_id", "User"
    %th=sortable "priority"
    %th=sortable "app"
    %th=sortable "app_data", "App data"
    %th
    %th
    %th= link_to image_tag("my_images/add.png", :alt => "New", :size => "24x24"), new_device_path, :class => 'popme'

  = render :partial => 'device', :collection => @devices

%br
  = will_paginate
