.column 
  .span-4
    .field
      = f.label :user_id
  .span-8
    = f.select 'user_id', User.all.collect{|x| [x.full_name, x.id]}

.column 
  .span-4
    .field
      = f.label :exten
  .span-8
    = f.text_field :exten

.column 
  .span-4
    .field
      = f.label :priority
  .span-8
    = f.text_field :priority

.column 
  .prepend-9
    = f.submit 'Save', :id => 'submit_button'
