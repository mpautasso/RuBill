#header-popup
  %h1=t(:add_device)
  %br
.container{:style => "width:500px"}
  = form_for @device, :url => devices_path, :remote => true do |f|
    #errors

    = render :partial => 'form', :locals => {:f => f}
