%h1.title=t(:devices)

= form_tag devices_path, :method => 'get', :id => "devices_search" do
  %p
    = text_field_tag :search, params[:search], :placeholder => t(:exten_search)
    #submit
      .button
        = submit_tag t(:search), :exten => nil
  %p
  #devices_table
    = render 'devices'
