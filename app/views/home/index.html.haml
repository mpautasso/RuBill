#block-text.block
  .content
    %h2.title Dashboard
  
  RuBill is a simple interface to administrate the billing costs.
  .inner
    %p.first
      .column
        - if current_user.admin?
          %b
            Import Calls
          %br
          Clicking on 'Import Calls' will grab calls from db/calls.csv 
          %br
          %br
          = button_to "Import Calls", import_path(), :id =>'import_button'
          %br
          #progressbar.hide
      .append-12
        .prepend-13
          = link_to logo


- content_for(:info) do
  Welcome to your dashboard...

:javascript

  $(document).ready(function() {
    $("#import_button").click( function(){
      $("#progressbar").show();
      $("#progressbar").progressbar({ value: 90 });
    });
  });
  
  $(function() {
    $( "#progressbar" ).progressbar({value: 30});
  });
