#block-text.block
  .content
    %h2.title=t(:welcome_rubill)

  %p.nice_text
    RuBill is a simple interface to administrate your billing calls.

  .inner
    %p.first
      %hr.space
      %hr.space
      %hr.space
      %hr.space


  .column
    = link_to logo

:javascript
  $(document).ready(function() {
    $("#import_button").click( function(){
      $.ajax({
          type: "POST",
          url: '/import',
          beforeSend: function(){
            $("#import_spin").show();
          },
          complete: function() {
            $("#import_spin").hide();
          }
      });

      return false;
    });
  });

