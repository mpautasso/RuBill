!!!Strict
%html
  %head
    %title="Rubill"

    = stylesheet_link_tag :all
    = javascript_include_tag :defaults
    = csrf_meta_tag

%body
  - if current_user 
    = link_to "logout", destroy_user_session_url()
  - else
    = link_to "logout", destroy_user_session_url()
    
  = yield
