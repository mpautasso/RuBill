- if obj.errors.any?
  #error_explanation
    %h2= "#{pluralize(obj.errors.count, "errores")} #{t(:prohibited)} #{obj.class.to_s.downcase} #{t(:behin_saved)}:"
    %p=t(:were_problems) 
    %ul
      - obj.errors.full_messages.each do |msg|
        %li= msg
