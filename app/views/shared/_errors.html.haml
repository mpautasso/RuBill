- if obj.errors.any?
  #error_explanation
    %h2= "#{pluralize(obj.errors.count, "error")} prohibited this #{obj.class.to_s.downcase} from being saved:"
    %p= "There were problems with the following fields:"
    %ul
      - obj.errors.full_messages.each do |msg|
        %li= msg
