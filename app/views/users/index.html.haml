%h1 Listing Users

= form_tag users_path, :method => 'get', :id => "users_search" do
  %p
    = text_field_tag :search, params[:search]
    = submit_tag "Search", :fullname => nil
  %p
  #users_table
    = render 'users'

  %p
    = link_to 'New User', new_user_path, :class => 'popme'
