%h1.title=t(:users)

= form_tag users_path, :method => 'get', :id => "users_search" do
  %p
    = text_field_tag :search, params[:search], :placeholder => t(:lastname_search)
    #submit
      .button
        = submit_tag t(:search), :last_name => nil
  %p
  #users_table
    = render 'users'

