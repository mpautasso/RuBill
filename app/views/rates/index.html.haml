%h1.title Rates

= form_tag rates_path, :method => 'get', :id => "rates_search" do
  %p
    = text_field_tag :search, params[:search], :placeholder => 'Enter prefix to search...'
    #submit
      .button
        = submit_tag "Search", :prefix => nil
  %p
  #rates_table
    = render 'rates'
