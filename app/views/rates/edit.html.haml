#header-popup
  %h1=t(:edit_rate)
  %br
.container{:style => "width:600px"}
  = form_for @rate, :url => rate_path(@rate), :remote => true do |f|
    #errors

    = render :partial => 'form', :locals => {:f => f}
