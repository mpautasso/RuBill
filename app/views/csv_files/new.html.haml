#header-popup
  %h1 New CSV file
  %br
.container{:style => "width:500px"}
  = form_for @csv_file, :url => csv_files_path, :remote => true do |f|
    #errors

    = render :partial => 'form', :locals => {:f => f}
