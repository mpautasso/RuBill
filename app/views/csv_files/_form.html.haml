= f.hidden_field 'user_id', :value => current_user.id

.span-2 
  .field
    = f.label :csv, 'CSV file:'
.span-6 
  = f.file_field :csv

%br
.column 
  .prepend-8
    = f.submit 'Save', :id => 'submit_button'

