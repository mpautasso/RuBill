#header-popup
  %h1=t(:edit_failedcall)
  %br
.container{:style => "width:500px"}
  = form_for @failed_call, :url => failed_call_path(@failed_call), :remote => true do |f|
    #errors

    = render :partial => 'form', :locals => {:f => f}
