= hidden_field_tag :direction, params[:direction]
= hidden_field_tag :sort, params[:sort]
 
%table#failedcalls
  %tr.pretty.ajaxifyme
    %th= sortable "calldate", "Call date"
    %th= sortable "src", "Source"
    %th= sortable "dst", "Destination"
    %th= sortable "duration", "Duration"
    %th= sortable "billsec", "Bill seconds"
    %th
    %th= link_to image_tag("my_images/add.png", :alt => "New", :size => "24x24"), new_failed_call_path, :class => 'popme'

  = render :partial => 'failed_call', :collection => @failed_calls      
  
%br
  = will_paginate
  
:javascript
  $(function() {
    $("a.popme").fancybox({
          'padding':  7
    });
  });
