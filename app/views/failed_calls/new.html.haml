#header-popup
  %h1 New failed call
  %br
.container{:style => "width:500px"}
  = form_for @failed_call, :url => failed_calls_path, :remote => true do |f|
    #errors

    = render :partial => 'form', :locals => {:f => f}
